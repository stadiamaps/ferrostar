{
  "name": "ferrostar-components",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "module": "./dist/ferrostar-components.js",
  "exports": {
    ".": "./dist/ferrostar-components.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest",
    "prepare:core": "cd ../common && wasm-pack build --target web ferrostar --no-default-features --features wasm_js",
    "build": "npm run prepare:core && tsc && vite build",
    "build:site": "npm run build && vite build --config vite.config.site.ts"
  },
  "dependencies": {
    "@stadiamaps/maplibre-search-box": "^1.0.1",
    "ferrostar": "file:../common/ferrostar/pkg",
    "lit": "^3.1.4",
    "maplibre-gl": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.4.1",
    "vitest": "^2.0.5"
  }
}
