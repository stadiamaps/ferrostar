import SwiftUI

struct ConfigurationView: View {
    var body: some View {
        VStack(spacing: 15) {
            Text("TODO basic config for demo")
        }
        .navigationTitle("Config")
    }
}

#Preview {
    ConfigurationView()
}
